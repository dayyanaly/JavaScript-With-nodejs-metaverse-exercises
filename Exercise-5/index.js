let person = "Albert Einstein";
let quote = "“We cannot solve our problems with the same thinking we used when we created them.”";
let message = `${person} once said, ${quote}`;

document.write(`<h1>${message}</h1>`);